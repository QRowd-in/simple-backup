# ============================================================
# simple-backup configuration
# ============================================================
# On Railway, set these as service variables in the dashboard.
# For DATABASE_URL, use the reference variable: ${{Postgres.DATABASE_URL}}
# ============================================================

# ----- Required -----

# Postgres connection string
DATABASE_URL=postgresql://user:password@host:5432/dbname

# S3 bucket name
S3_BUCKET=my-backup-bucket

# S3 credentials
S3_ACCESS_KEY_ID=your-access-key
S3_SECRET_ACCESS_KEY=your-secret-key

# ----- Optional -----

# S3 region (default: us-east-1)
# S3_REGION=us-east-1

# Custom S3 endpoint (for Cloudflare R2, MinIO, DigitalOcean Spaces, etc.)
# S3_ENDPOINT=https://0ee2db257ceef1f0c9346692936aa2bd.r2.cloudflarestorage.com

# S3 key prefix for backup files (default: backups/{dbname}/)
# BACKUP_PREFIX=backups/my-database/

# Maximum number of backups to retain (default: 30)
# BACKUP_MAX_COUNT=30

# Seconds to wait for Postgres to become reachable (default: 120)
# Increase this if your serverless Postgres takes a long time to wake up
# PG_CONNECT_TIMEOUT=120
